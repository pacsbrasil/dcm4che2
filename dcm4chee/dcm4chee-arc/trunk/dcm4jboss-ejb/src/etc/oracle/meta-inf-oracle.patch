--- target/xdoclet/META-INF-hsql/jbosscmp-jdbc.xml	2005-02-21 17:16:50.000000000 +0100
+++ target/xdoclet/META-INF-oracle/jbosscmp-jdbc.xml	2005-02-21 17:32:09.000000000 +0100
@@ -3,8 +3,8 @@
 
 <jbosscmp-jdbc>
    <defaults>
-     <datasource>java:/DefaultDS</datasource>
-     <datasource-mapping>Hypersonic SQL</datasource-mapping>
+     <datasource>java:/OracleDS</datasource>
+     <datasource-mapping>Oracle9i</datasource-mapping>
      <create-table>false</create-table>
    </defaults>
 
@@ -68,7 +68,8 @@
 
         </cmp-field>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">mwl_item_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -165,7 +166,8 @@
 			</read-ahead>
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">instance_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -216,7 +218,8 @@
 			</read-ahead>
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">ae_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -276,7 +279,8 @@
 			</read-ahead>
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">device_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -338,7 +342,8 @@
 			</read-ahead>
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">patient_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -522,7 +527,8 @@
 
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">study_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -679,7 +685,8 @@
 
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">series_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -722,7 +729,8 @@
 
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">study_on_fs_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -834,7 +842,8 @@
 
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">media_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -889,7 +898,8 @@
 			</read-ahead>
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">mpps_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -941,7 +951,8 @@
 			</read-ahead>
 	     </query>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">files_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -979,7 +990,8 @@
 
         </cmp-field>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">code_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
@@ -1007,7 +1019,8 @@
 
         </cmp-field>
 
-	<entity-command name="hsqldb-fetch-key">
+	<entity-command name="oracle-table-sequence" class="org.jboss.ejb.plugins.cmp.jdbc.keygen.JDBCOracleCreateCommand">
+	     <attribute name="sequence">filesystem_pk_seq</attribute>
 	</entity-command>
 <!-- jboss 3.2 features -->
 <!-- optimistic locking does not express the exclusions needed -->
