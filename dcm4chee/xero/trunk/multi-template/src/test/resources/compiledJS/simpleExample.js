/** This is what the "compiled" version of simpleExample.tml should look like.
    Note that this is a javascript file, not an original template. 
  */
  
function simpleExample(templates) {
};
simpleExample.prototype = new Initial();

function simpleExample_merge(context) {
	return "<html><body></body></html>";
};
