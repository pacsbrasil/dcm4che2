$unittest()$
$model()$

/** This test just checks to see that the model javascript can be 
 * interpreted and is valid JavaScript - no actual tests are done, but the
 * full JavaScript object should be defined as used by the client.
 */
var modelInterpretTest = new UnitTest();
